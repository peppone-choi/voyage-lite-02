spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        
logging:
  level:
    kr.hhplus.be.server: DEBUG
    org.hibernate: DEBUG
    org.springframework.orm.jpa: DEBUG
    root: INFO